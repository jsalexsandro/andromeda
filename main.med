identity<string>("hello")
identity<int | null>(null)
identity<{ id: int; name: string }>({ id: 1, name: "a" })
identity<readonly string[]>(["a", "b"])
map<string, int>(["a", "b"])
nested<Array<string | null>>([])
chain<string>()<int>()
obj.method<string>("hello")
arr.map<string>(x => x.toString())