// Array tridimensional de funções que retornam tuplas (se você implementou TupleType)
// ou arrays de objetos.
val matrix: ((int) => { x: int, y: int }[][])[][] = []

// Objeto cujas propriedades são arrays de objetos com métodos que retornam arrays
val system: {
    layers: {
        nodes: { 
            getValue: () => int[] 
        }[]
    }[]
} = { layers: [] }

// Acesso bizarro
val result = system.layers[0].nodes[5].getValue()[0] 
