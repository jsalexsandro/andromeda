const data = {
    users: [
        { 
            config: () => ({ env: "prod" }),
            tags: ["admin", "staff"]
        }
    ]
}

// Acesso encadeado complexo
val env = data.users[0].config().env
val isStaff = data.users[0].tags[1] == "staff"

// Spread com propriedades calculadas (se o seu lexer suportar strings como chaves)
const updated = { ...data.users[0], lastLogin: "2026-04-15" }
