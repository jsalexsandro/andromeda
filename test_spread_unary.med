// Spreads aninhados e operadores de negação/sinal
const base = { a: 1 }
const deepCopy = { ...{ ...{ ...{ ...base } } } }

val list = [1, 2, 3]
val megaList = [...[...[...list, ...list], 4], 5]

// Isso deve ser aceito: negação tripla e soma de unários
val crazyMath = !!!true == !!false
val signMadness = - - - + + -5 
